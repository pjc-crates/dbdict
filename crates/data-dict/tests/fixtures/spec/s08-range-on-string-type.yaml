# expected: fails S07 - `range` is only allowed on ordered numeric / date / datetime
# columns, not on strings.

$version: 0.1.0

tables:
  t:
    source:
      parquet: t.parquet
    columns:
      - name: c
        type: string
        range: ["a", "z"]
